#!/usr/bin/ruby

# Given a preconfigured path (or the environment variable BACKGROUNDS),
# recursively scans through the directory for all jpegs and pngs and randomly
# selects one. Then call `feh --xinerama --bg-scale <file>`
